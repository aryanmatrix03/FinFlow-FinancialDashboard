/**
 * icons.js — single import point for every Lucide icon used in FinFlow.
 * Swap the icon library here without touching any component file.
 */
export {
  // ── Sidebar nav ──────────────────────────────
  LayoutDashboard,
  ArrowLeftRight,
  BarChart3,

  // ── Theme ────────────────────────────────────
  Sun,
  Moon,
  LogOut,

  // ── Mobile nav ───────────────────────────────
  Menu,

  // ── TopBar header actions ────────────────────
  Bell,
  Settings,
  HelpCircle,

  // ── Settings dropdown items ───────────────────
  UserCircle,
  Globe,
  Upload,
  ShieldCheck,

  // ── Help dropdown items ───────────────────────
  BookOpen,
  AlertCircle,
  Lightbulb,

  // ── Notification type icons ───────────────────
  CheckCircle,
  AlertTriangle,
  Info,

  // ── Card quick-actions ───────────────────────
  ArrowUpRight,
  ArrowDownLeft,
  Copy,
  ShieldOff,
  Eye,
  EyeOff,

  // ── Insight metric cards ─────────────────────
  Wallet,
  PieChart,
  RefreshCw,

  // ── KPI trend ────────────────────────────────
  TrendingUp,
  TrendingDown,

  // ── Empty states ─────────────────────────────
  CreditCard,
  SearchX,
  ClipboardList,

  // ── Misc ─────────────────────────────────────
  ChevronRight,
  X,
  Check,
  ArrowUp,
  ArrowDown,
} from "lucide-react";
